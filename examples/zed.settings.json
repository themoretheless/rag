// Shared-gateway snippet for ~/.config/zed/settings.json
// (or Settings → AI → MCP Servers → Add Remote Server)
// Docs: https://zed.dev/docs/ai/mcp
// Merge `context_servers` into your existing settings (do not replace the whole file).
{
  "context_servers": {
    "rag-mcp": {
      "url": "http://127.0.0.1:7432/mcp"
    }
  }
}
